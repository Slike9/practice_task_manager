# Postgresql version 9.x
#   gem install pg
#
#   Ensure the Postgresql 9.x gem is defined in your Gemfile
#   gem 'pg'
development: &development
  adapter: postgresql
  encoding: utf8
  database: practice_task_manager_development
  username: postgres
  host: localhost
  port: 5432

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: practice_task_manager_test
  username: postgres
  host: localhost
  port: 5432

production:
  adapter: postgresql
  encoding: utf8
  database: practice_task_manager_production
  username: postgres
  host: localhost
  port: 5432