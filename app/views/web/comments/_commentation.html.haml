.commentation
  = render partial: 'web/comments/form', locals: {comment: Comment.new, story: story}

  %h2= t('.comments_title')
  = render partial: 'web/comments/comment', collection: story.comments.ordered_by_time
