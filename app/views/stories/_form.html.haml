= simple_form_for(@story) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.association :owner, collection: User.all

  .form-actions
    = f.button :submit
